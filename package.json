{
  "name": "titleRegistryV2",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ title-registry-v2",
    "create-local": "graph create --node http://localhost:8020/ titleRegistryV2",
    "remove-local": "graph remove --node http://localhost:8020/ titleRegistryV2",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 titleRegistryV2",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.42.4",
    "@graphprotocol/graph-ts": "0.28.1"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
